<h1>テナント: <%= @tenant.name %></h1>

<h2>投稿一覧</h2>
<ul>
  <% @posts.each do |post| %>
    <li><%= link_to "投稿 #{post.id} - #{post.body.truncate(50)}", post %></li>
  <% end %>
</ul>

<h2>ユーザー一覧</h2>
<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.name, user %></li>
  <% end %>
</ul>
